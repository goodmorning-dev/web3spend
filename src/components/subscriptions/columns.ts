/** Column widths shared by SubscriptionRow and the Subscriptions page's list
 * header, so the column labels line up with the rows underneath them. */
export const COLUMN_CLASS = {
  months: 'hidden w-[164px] shrink-0 md:flex',
  paid: 'hidden w-[118px] shrink-0 flex-col items-end lg:flex',
  status: 'hidden w-[150px] shrink-0 sm:flex',
  price: 'flex w-[84px] shrink-0 flex-col items-end',
}
